# Empty conftest at the project root.
#
# Its only job is to mark this directory as the test root so that `pytest`
# (run from here) can import the `simulated_data` package without any extra
# setup -- just run `pytest`.
